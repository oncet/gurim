{% extends 'layout.twig' %}

{% block content %}
    <div class="content">
        {% if post.title %}
            {% include 'partials/h1.twig' with {
                title: post.title
            } %}
        {% endif %}
        {{ post.content }}
    </div>
{% endblock %}