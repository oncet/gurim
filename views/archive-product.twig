{% extends 'layout.twig' %}

{% block content %}
    <div class="products">
        <div class="row">
            {% for product in products %}
                {% include 'partials/item.twig' with {
                    post: product
                } %}
            {% endfor %}
        </div>
    </div>
    {% include 'partials/pagination.twig' with {
        paginable: products
    } %}
{% endblock %}